import React from 'react';

const SelectedPlayers = ({setSelectedPlayers, selectedPlayers}) => {
    console.log(selectedPlayers);
    return (
        <div>
            
        </div>
    );
};

export default SelectedPlayers;